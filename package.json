{
  "name": "cypress-bugasura-demo1",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
  "test": "cypress run --spec 'cypress/e2e/todo.cy.js'",
  "report:bugasura": "bugasura UPLOAD_RESULTS ./cypress/reports/junit/todo.cy.js-results.xml --api_key $BUGASURA_API_KEY --team_id $BUGASURA_TEAM_ID --project_id $BUGASURA_PROJECT_ID --testrun_id $BUGASURA_TESTRUN_ID --server $BUGASURA_SERVER",
  "test:full": "npm run test && npm run report:bugasura"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.5.3",
    "dotenv-cli": "^9.0.0",
    "mocha": "^11.7.1",
    "mocha-junit-reporter": "^2.2.0"
  }
}
